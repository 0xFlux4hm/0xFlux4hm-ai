# 0xFlux4hm AI — VulnAgentRouter Dependencies
# Install with: pip install -r requirements.txt

# Core AI
anthropic>=0.25.0

# Environment management
python-dotenv>=1.0.0

# Terminal colors and formatting
colorama>=0.4.6
rich>=13.7.0

# HTTP requests (for Exploit-DB API, web probing)
requests>=2.31.0
httpx>=0.27.0

# YAML configuration
PyYAML>=6.0.1

# Database (SQLite ORM)
# sqlite3 is built-in to Python, but we use this for cleaner ORM
# No extra package needed; stdlib sqlite3 is used directly

# HTML report generation
jinja2>=3.1.4
markdown>=3.6

# DNS resolution / subdomain enumeration helpers
dnspython>=2.6.1

# Date/time utilities (stdlib, no install needed)
# Included for completeness documentation

# Optional: For async scanning (future extension)
# aiohttp>=3.9.5
